// Este metodo funciona para validar si una variable
// es un arreglo o una matriz
let objeto = {};
let arreglo = [];
// Dentro del metodo se coloca la variable que
// queremos validar si es un arreglo
console.log(Array.isArray(objeto));
console.log(Array.isArray(arreglo));
