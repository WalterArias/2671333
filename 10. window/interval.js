// intervalos hay una accion recurrente que se ejecuta en una ventana de tiempo
/* 
let saludar = () => {
  return document.write(`hola js <br>`);
};

setInterval(saludar, 3000); // usamos una funcion callback a ejecutarse en 6 segs
 */

console.log(navigator.userAgent);
